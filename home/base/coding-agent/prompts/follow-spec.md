---
description: Implement a function from a markdown specification in a new worktree place
argument-hint: [SPEC=<paths>]
---

Tasks:

1. Create a new worktree by `git worktree` in a parent dir.
2. Change current dir to the worktree path.
3. Implement a function from a markdown specification ($SPEC)
   - Keep the implementation minimal and aligned with existing project style.
